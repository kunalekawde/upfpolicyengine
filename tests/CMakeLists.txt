file(GLOB_RECURSE UPF_TEST_SOURCES CONFIGURE_DEPENDS unit/*.cpp state/*.cpp contract/*.cpp scenario/*.cpp property/*.cpp)
add_executable(upf_tests ${UPF_TEST_SOURCES})
target_link_libraries(upf_tests PRIVATE upf_core Catch2::Catch2)
target_include_directories(upf_tests PRIVATE ${PROJECT_SOURCE_DIR})
target_compile_features(upf_tests PRIVATE cxx_std_11)
upf_enable_warnings(upf_tests)
upf_enable_sanitizers(upf_tests)
upf_enable_coverage(upf_tests)
add_test(NAME upf_tests COMMAND upf_tests)

if(UPF_DEMO_BUILD_FUZZERS AND CMAKE_CXX_COMPILER_ID MATCHES "Clang")
  add_executable(upf_input_fuzzer fuzz/input_fuzzer.cpp)
  target_link_libraries(upf_input_fuzzer PRIVATE upf_core)
  target_compile_options(upf_input_fuzzer PRIVATE -fsanitize=fuzzer,address,undefined)
  target_link_options(upf_input_fuzzer PRIVATE -fsanitize=fuzzer,address,undefined)
endif()
